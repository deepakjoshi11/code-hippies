node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
.env
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo
.lighthouseci
/data/chat-gaps.log

# Vercel CLI appends `.env*`; keep .env.example tracked — it is the
# documentation for every variable and must stay in the repository.
.env*
!.env.example
